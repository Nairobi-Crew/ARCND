import ChangePassword from 'Pages/ChangePassword/ChangePassword';
import { memo } from 'react';
import { OwnChangePasswordProps } from 'Pages/ChangePassword/types';

export default memo<OwnChangePasswordProps>(ChangePassword);
