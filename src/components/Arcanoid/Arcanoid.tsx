import React from 'react';
import Canvas from 'Components/Arcanoid/Canvas/index';
import { GameScreenProps } from 'Components/Arcanoid/types';

export const Arcanoid: GameScreenProps = () => (
  <Canvas />
);
export default Arcanoid;
